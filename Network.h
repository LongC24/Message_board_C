#ifndef Message_board_NETWORK_H
#define Message_board_NETWORK_H


class Network {
public:
// If the network is connected, return true. Otherwise, return false.
    static bool isConnected();
};



#endif 
